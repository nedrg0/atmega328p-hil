on: [push]
jobs:
  build_and_static_analysis:
    runs-on: ubuntu-latest
    container:
      image: nedrag/atmega328-gcc:latest
      options: --user root
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v3
      - run: make
      - run: make cppcheck 
